# Direct dependencies for LuigiBot
discord.py>=2.6,<3.0
pandas>=2.0
pytz>=2024.1
matplotlib>=3.8

# Persistence engine (SQLite always, Postgres opt-in via LUIGI_DB_BACKEND)
SQLAlchemy>=2.0
psycopg[binary]>=3.1

# Loads <repo>/.env into os.environ at import (see bot_modules/bot_config.py).
python-dotenv>=1.0

# Optional: notebook tools used in to_do_list/*.ipynb playgrounds
ipykernel>=6.0
